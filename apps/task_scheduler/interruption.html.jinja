<p class="scheduler-task-copy">
  <strong>{{ worker }} interrupted</strong> · offline for {{ "%.0f" | format(outage) }} simulated seconds.
  <strong>{{ task }}</strong> returned to the ready queue; the empty interval in this worker lane is the outage.
</p>
