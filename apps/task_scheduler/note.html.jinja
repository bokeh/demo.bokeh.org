<p>
  <strong>Simulation:</strong> all task graphs, durations, failures, and worker activity are synthetic.
  Seed <code>{{ seed }}</code> keeps each restart reproducible. Hover and tap remain local browser
  interactions while scheduling advances in Python.
</p>
